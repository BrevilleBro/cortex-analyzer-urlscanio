{
    "name": "UrlScan_Scan",
    "version": "1.0",
    "author": "UNIT777",
    "url": "https://urlscan.io/about-api/",
    "license": "AGPL-V3",
    "description": "Analyses websites and the resources they request. Much like the Inspector of your browser, urlscan.io will let you take a look at the individual resources that are requested when a site is loaded.",
    "dataTypeList": ["domain", "url"],
    "baseConfig": "UrlScan",
    "config": {
        "check_tlp": true,
        "max_tlp": 1,
        "service": "scan"
    },
    "configurationItems": [{
        "name": "key",
        "description": "API key for urlscan.io",
        "type": "string",
        "multi": false,
        "required": true
    }, {
        "name": "public",
        "description": "Input text 'off' or 'on' for making the scans private or public",
        "type": "string",
        "multi": false,
        "required": true
    }],
    "command": "UrlscanIO/urlscan.py"
}
